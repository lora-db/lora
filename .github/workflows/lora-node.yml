name: lora-node

on:
  push:
    branches: [main]
    paths:
      - "crates/lora-node/**"
      - "crates/lora-ast/**"
      - "crates/lora-parser/**"
      - "crates/lora-analyzer/**"
      - "crates/lora-compiler/**"
      - "crates/lora-executor/**"
      - "crates/lora-store/**"
      - "crates/lora-database/**"
      - "crates/shared-ts/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".github/actions/setup-rust/**"
      - ".github/workflows/lora-node.yml"
  pull_request:
    paths:
      - "crates/lora-node/**"
      - "crates/lora-ast/**"
      - "crates/lora-parser/**"
      - "crates/lora-analyzer/**"
      - "crates/lora-compiler/**"
      - "crates/lora-executor/**"
      - "crates/lora-store/**"
      - "crates/lora-database/**"
      - "crates/shared-ts/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".github/actions/setup-rust/**"
      - ".github/workflows/lora-node.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    working-directory: crates/lora-node

jobs:
  verify:
    name: build, typecheck, test, npm pack (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-rust
        with:
          cache-key: lora-node-${{ matrix.os }}

      - uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: crates/lora-node/package-lock.json

      - name: cargo check
        working-directory: .
        run: cargo check -p lora-node --locked

      - name: npm install
        run: npm ci --no-audit --no-fund --ignore-scripts

      - name: verify shared TS types are in sync
        run: npm run verify:types

      - name: build native (napi)
        run: npm run build:native

      - name: build TS
        run: npm run build:ts

      - name: typecheck
        run: npm run typecheck

      - name: vitest (includes non-blocking event-loop test)
        run: npm test

      - name: npm pack (dry-run)
        run: npm pack --dry-run

      - name: npm pack
        run: npm pack

      - uses: actions/upload-artifact@v5
        with:
          name: lora-node-${{ matrix.os }}-tarball
          path: crates/lora-node/loradb-lora-node-*.tgz
          if-no-files-found: error
          retention-days: 7
