name: lora-wasm

on:
  push:
    branches: [main]
    paths:
      - "crates/lora-wasm/**"
      - "crates/lora-ast/**"
      - "crates/lora-parser/**"
      - "crates/lora-analyzer/**"
      - "crates/lora-compiler/**"
      - "crates/lora-executor/**"
      - "crates/lora-store/**"
      - "crates/lora-database/**"
      - "crates/shared-ts/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".github/actions/setup-rust/**"
      - ".github/workflows/lora-wasm.yml"
  pull_request:
    paths:
      - "crates/lora-wasm/**"
      - "crates/lora-ast/**"
      - "crates/lora-parser/**"
      - "crates/lora-analyzer/**"
      - "crates/lora-compiler/**"
      - "crates/lora-executor/**"
      - "crates/lora-store/**"
      - "crates/lora-database/**"
      - "crates/shared-ts/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".github/actions/setup-rust/**"
      - ".github/workflows/lora-wasm.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    working-directory: crates/lora-wasm

jobs:
  verify:
    name: build, typecheck, test, npm pack
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-rust
        with:
          cache-key: lora-wasm-ubuntu
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.4.0
        with:
          version: "v0.14.0"

      - uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: crates/lora-wasm/package-lock.json

      - name: cargo check (host)
        working-directory: .
        run: cargo check --workspace --exclude lora-node --locked

      - name: cargo check (wasm32)
        working-directory: .
        run: cargo check -p lora-wasm --target wasm32-unknown-unknown --locked

      - name: npm install
        run: npm ci --no-audit --no-fund --ignore-scripts

      - name: verify shared TS types are in sync
        run: npm run verify:types

      - name: build WASM (node + bundler + web)
        run: npm run build:wasm

      - name: build TS
        run: npm run build:ts

      - name: typecheck (tests, examples, configs)
        run: npm run typecheck

      - name: vitest
        run: npm test

      - name: install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Playwright browser smoke test
        run: npm run test:browser

      - name: npm pack (dry-run)
        run: npm pack --dry-run

      - name: npm pack (produce tarball)
        run: npm pack

      - uses: actions/upload-artifact@v5
        with:
          name: lora-wasm-tarball
          path: crates/lora-wasm/lora-wasm-*.tgz
          if-no-files-found: error
          retention-days: 7
